# g_search

A simple CLI tool that searches for a query string within a file and prints matching lines with line numbers.

## Usage

```bash
go run main.go <query> <file_path>
```

## Example

```bash
go run main.go "function" main.go
```
